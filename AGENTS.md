# 행사 참가 신청 사이트 개발 지침

## 1. 프로젝트 목표

Next.js와 Supabase를 사용해 다음 서비스를 구축한다.

- 행사 정보를 안내하는 반응형 원페이지
- 팀 단위 참가 신청 및 증빙 이미지 제출
- 팀명과 신청 비밀번호를 이용한 신청 확인·수정
- Supabase Auth 기반 관리자용 신청 관리
- SheetJS 기반 엑셀 가져오기·내보내기
- 네이버 SMTP 기반 신청 완료 이메일 발송
- Vercel 배포

사용자가 명시하지 않은 신청 항목, 운영 규칙, 화면 또는 기능을 임의로 추가하지 않는다.

## 2. 구현 전 필수 게이트

코드, 스키마, 마이그레이션 또는 UI를 작성하기 전에 아래 내용을 사용자에게 질문하고 답을 확정해야 한다. 답변 전에는 구현을 시작하지 않는다.

1. 행사명, 일정, 장소, 핵심 소개 및 상세 소개
2. 실제 `참가 신청 정보`에 포함할 정확한 입력 항목과 각 항목의 필수 여부
3. 제출받을 증빙 이미지의 종류와 용도
4. 증빙 이미지 최대 개수와 파일당 최대 용량
5. HEIC 지원 여부
   - 서버 및 브라우저 호환성, 변환 필요성, 저장 형식을 검토한 뒤 선택지를 설명하고 확인한다.
6. 팀명 중복 허용 여부
   - 불허 시 공백, 대소문자, 유니코드 및 특수문자 정규화 기준도 확인한다.
7. 신청 시작·마감 일시와 수정 가능 기간
8. 관리자 계정 수와 관리자 권한 구분 필요 여부
9. 이메일 발신 도메인, 발신자 이름, 문의처
10. 개인정보 보관 기간, 삭제 기준 및 삭제 방식
11. 엑셀 다운로드·업로드에 포함할 정확한 열 목록

질문에 대한 답변을 받은 다음, 곧바로 구현하지 말고 먼저 다음 두 산출물을 사용자에게 제안하고 승인을 받는다.

- 최종 기능 명세 및 사용자 흐름
- 데이터 구조, 권한 구조, 접근 정책 및 주요 운영 규칙

## 3. 기술 구성

- 프론트엔드 및 서버: Next.js App Router + TypeScript
- 스타일: Tailwind CSS
- 데이터베이스: Supabase PostgreSQL
- 이미지 저장: Supabase Storage의 비공개 버킷
- 이메일: 네이버 SMTP (nodemailer)
- 엑셀: SheetJS (`.xlsx`)
- 입력 검증: Zod, 클라이언트와 서버 모두 적용하되 서버 검증을 신뢰 경계로 삼는다.
- 신청 비밀번호: argon2 또는 bcrypt 해시만 저장한다.
- 관리자 인증: Supabase Auth
- 배포: Vercel
- 운영 시간 표시: `Asia/Seoul`
- 데이터베이스 시각 저장: UTC 기반으로 저장하고 표시·입력 경계에서 `Asia/Seoul`로 명시적으로 변환한다.

서비스 역할 키, SMTP 계정 정보 등 비밀값은 서버 전용 환경 변수로 관리하며 브라우저 번들에 포함하지 않는다.

## 4. 페이지와 사용자 흐름

### `/` 메인 페이지

다음 영역을 포함하는 반응형 원페이지로 만든다.

- 행사명과 핵심 소개
- 행사 상세 소개
- 일정 및 장소
- 참가 대상과 조건
- 신청 방법
- 제출 자료 안내
- 자주 묻는 질문
- `/apply`로 이동하는 참가 신청 버튼
- `/application`으로 이동하는 신청 확인·수정 버튼

확정되지 않은 행사 정보는 임의의 실제 문구로 채우지 않는다. 쉽게 교체 가능한 설정 데이터로 분리하고, 필요한 경우 명백한 미설정 상태로 표현한다.

### `/apply` 참가 신청

확정된 신청 항목만 구현한다. 기본 항목은 다음과 같다.

- 팀명
- 신청 비밀번호
- 비밀번호 확인
- 대표자 이름
- 대표자 이메일
- 대표자 연락처
- 사용자와 확정한 참가 신청 정보
- 증빙자료 이미지 업로드
- 개인정보 수집·이용 동의
- 제출 버튼

참가자 사진 업로드 기능은 만들지 않는다.

증빙자료는 JPG/JPEG, PNG, WEBP 이미지만 기본 허용한다. HEIC는 사용자의 명시적 결정 후에만 지원한다. 확장자와 MIME 타입을 모두 검사하고, 확정된 최대 파일 개수와 파일당 용량을 서버에서도 검증한다.

제출은 다음 순서를 보장한다.

1. 서버에서 모든 입력과 파일을 다시 검증한다.
2. 확정된 정규화 규칙에 따라 팀명 중복 여부를 확인한다.
3. 비밀번호를 안전하게 해싱한다.
4. 신청 데이터와 접수번호를 저장한다.
5. 증빙 이미지를 비공개 Storage에 업로드하고 파일 메타데이터를 저장한다.
6. 데이터와 이미지 저장이 모두 성공한 경우에만 신청 완료 이메일을 1회 발송한다.
7. 완료 화면에 접수번호를 표시한다.

부분 실패 시 일관성을 보장하도록 보상 처리 또는 명시적인 제출 상태를 설계한다. 데이터 저장이나 이미지 업로드가 실패하면 완료 이메일을 보내지 않는다. 같은 제출 요청이 재전송되어도 중복 신청과 중복 이메일이 생기지 않도록 고유 요청 키와 이메일 발송 상태를 사용한다.

### `/application` 신청 확인·수정

첫 화면에서는 팀명과 비밀번호만 입력받는다. 인증 실패 시 어느 값이 잘못됐는지 구분해 노출하지 않는다. 반복 실패에 대해 서버 측 횟수 제한과 일정 시간 잠금을 적용한다.

인증 성공 후 해당 팀의 정보에만 접근할 수 있어야 하며 다음을 제공한다.

- 기존 신청 내용 확인 및 수정
- 증빙 이미지 확인, 교체 및 삭제
- 마지막 수정 시각 확인
- 신청 마감 또는 수정 비활성화 시 읽기 전용 전환

사용자용 비밀번호 찾기와 비밀번호 재설정 기능은 만들지 않는다.

### `/admin/login`, `/admin/applications`

관리자 화면과 경로는 일반 사용자 UI에 노출하지 않는다. 숨김만 보안 수단으로 사용하지 말고, 관리자 페이지와 모든 관리자 API에서 Supabase Auth 세션과 관리자 권한을 서버에서 검사한다.

관리 화면은 많은 데이터를 다룰 수 있는 표 형태로 만들고 서버 기반 페이지 이동 또는 서버 기반 목록 조회를 적용한다. 표 상단에는 작은 크기의 다음 컨트롤을 둔다.

- 검색창
- 신청일 필터
- 정렬 선택
- 엑셀 다운로드 버튼
- 엑셀 업로드 버튼

확정 범위의 관리 기능은 다음과 같다.

- 팀명, 대표자, 연락처 검색
- 신청 상세 확인 및 수정
- 증빙 이미지 미리보기 및 다운로드
- 신청 수정 잠금·해제
- 전체, 현재 검색·필터 결과 또는 선택 항목 엑셀 다운로드
- 엑셀 업로드 검증 및 반영 전 미리보기
- 관리자 작업 이력 확인

## 5. 신청 완료 이메일

신규 신청의 모든 저장 작업이 성공한 경우에만 대표자 이메일로 정확히 1회 발송한다.

포함 내용:

- 신청 완료 안내
- 접수번호
- 팀명
- 신청 완료 시각
- 신청 확인·수정 페이지 주소
- 문의 방법

비밀번호는 포함하지 않는다. 수정 완료, 관리자 신규 신청 알림, 홍보·마케팅 이메일은 발송하지 않는다.

고유 요청 키, `email_logs`의 고유 제약 또는 동등한 멱등성 장치로 중복 발송을 방지한다. 발송 실패와 재시도 정책은 최종 명세에서 사용자와 확정한다.

## 7. 엑셀 처리

SheetJS로 `.xlsx`만 처리한다.

다운로드 범위:

- 전체 신청
- 현재 검색·필터 결과
- 관리자가 선택한 신청

비밀번호 해시, 내부 Storage 경로, 관리자 정보, 토큰과 같은 보안·내부 데이터는 포함하지 않는다. 사용자와 확정한 열과 순서만 사용한다.

업로드 흐름:

1. 파일을 선택한다.
2. 헤더, 열, 데이터 형식을 검증한다.
3. 오류가 있는 행과 이유를 표시한다.
4. 신규, 수정, 변경 없음 항목을 구분해 미리 보여준다.
5. 관리자가 확인한 뒤에만 반영한다.
6. 업로드 결과, 처리 관리자, 처리 시각을 감사 로그에 기록한다.

기존 신청 식별에는 변경되지 않는 내부 신청 ID 또는 접수번호를 사용한다. 팀명만으로 기존 신청을 덮어쓰지 않는다.

## 8. 기본 데이터 구조

최종 컬럼은 신청 항목 확정 후 제안하고 승인받는다. 최소 테이블 책임은 다음과 같다.

- `applications`: 접수번호, 팀명, 비밀번호 해시, 대표자 정보, 확정된 신청 정보, 신청·수정 시각, 수정 잠금 등
- `application_files`: 신청 참조, 안전한 내부 저장 객체 키, 원본 파일명, MIME 타입, 크기, 생성 시각 등
- `email_logs`: 신청 참조, 이메일 종류, 멱등성 키, 발송 상태, 발송 시각, 안전한 오류 메타데이터 등
- `admin_audit_logs`: 관리자, 작업 종류, 대상, 처리 시각, 민감정보를 제외한 변경 요약 등
- `site_settings`: 신청 시작·마감, 수정 가능 여부, 사이트 공개 여부, 접수 완료 문구, 문의처, 신청 완료 이메일 내용 등

필요하면 관리자 권한 프로필, 요청 제한 또는 멱등성 처리를 위한 구조를 제안할 수 있으나 사용자 승인 없이 업무 기능을 추가하지 않는다.

## 9. 보안 및 접근 정책

- 신청 비밀번호는 argon2 또는 bcrypt 해시만 저장한다.
- 중요한 권한과 소유권 검사는 모두 서버에서 수행한다.
- 서비스 역할 키와 SMTP 계정 정보는 서버에서만 사용한다.
- 신청 데이터와 파일 메타데이터에 최소 권한 RLS 정책을 적용한다.
- 증빙 이미지 버킷은 비공개로 만들고 짧은 만료 시간의 서명 URL로만 조회·다운로드한다.
- 참가자는 인증 성공한 자기 신청에만 제한적으로 접근한다. 단순히 클라이언트가 보낸 신청 ID를 신뢰하지 않는다.
- 관리자 API는 매 요청마다 관리자 권한을 검증한다.
- 입력값, MIME, 파일 크기, 파일 개수, 엑셀 데이터는 서버에서 검증한다.
- 로그인, 신청, 파일 업로드 및 민감한 관리자 작업에 요청 횟수 제한을 적용한다.
- 개인정보, 비밀번호, 토큰, 서명 URL 및 파일 내용을 불필요하게 로그에 남기지 않는다.
- 관리자 변경은 감사 로그에 기록한다.
- 개인정보 보관·삭제 정책은 사용자와 확정한 기준으로 구현한다.

## 10. 운영 설정

관리자가 다음 값을 변경할 수 있게 한다.

- 신청 시작 일시
- 신청 마감 일시
- 신청 수정 가능 여부
- 사이트 공개 여부
- 접수 완료 안내 문구
- 문의처
- 신청 완료 이메일 내용

운영 화면에서는 `Asia/Seoul` 기준임을 명시한다. 저장 형식과 시간대 변환 규칙을 코드와 스키마에서 일관되게 유지한다.

## 11. 개발 순서

아래 순서를 지키며 한 번에 모든 화면을 만들지 않는다.

1. 미확정 신청 항목과 운영 규칙 질문 및 확정
2. 최종 기능 명세, 데이터 구조, 권한 구조 제안 및 승인
3. 페이지 구조와 사용자 흐름 정리 및 승인
4. Supabase 스키마, Storage 설정, RLS 접근 정책 작성
5. 참가 신청과 이미지 업로드 구현
6. 팀명+비밀번호 기반 조회·수정 구현
7. 신청 완료 이메일과 멱등성 구현
8. 관리자 화면, 운영 설정, 감사 로그 구현
9. 엑셀 다운로드·검증·미리보기·업로드 구현
10. 모바일 화면, 접근성, 오류 처리, 보안 점검
11. 실제 사용자·관리자 흐름 테스트 후 Vercel 배포

각 단계가 끝날 때 관련 검증을 수행하고 결과를 사용자에게 요약한다. 다음 단계가 앞선 미확정 결정에 크게 의존하면 먼저 사용자 확인을 받는다.

## 12. 구현 원칙과 완료 기준

- 사용자가 요청하지 않은 신청 필드나 기능을 추측해 추가하지 않는다.
- 예시 데이터가 필요해도 실제 행사 정보처럼 보이는 임의 문구를 확정값으로 넣지 않는다.
- 보안은 UI 숨김이나 클라이언트 검증에 의존하지 않는다.
- 중요한 쓰기 작업은 중복 요청과 부분 실패를 고려한다.
- 사용자에게 보이는 오류에는 개인정보와 내부 구현 정보를 노출하지 않는다.
- 모바일과 키보드 사용을 포함한 접근성을 확인한다.
- 빈 상태, 로딩, 검증 실패, 인증 실패, 네트워크 실패, 업로드 실패, 이메일 실패를 처리한다.
- 실제 사용 흐름 테스트에는 신규 신청, 중복 제출, 잘못된 로그인 반복, 마감 후 읽기 전용, 파일 교체·삭제, 관리자 권한 차단, 엑셀 미리보기·반영, 이메일 중복 방지가 포함되어야 한다.
- 배포 전 환경 변수, Supabase RLS, 비공개 Storage, 네이버 SMTP 계정·앱 비밀번호, Vercel 환경별 설정을 점검한다.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
